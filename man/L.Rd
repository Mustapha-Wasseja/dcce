% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_utils.R
\name{L}
\alias{L}
\title{Lag operator for dcce formulas}
\usage{
L(x, k = 1L)
}
\arguments{
\item{x}{A numeric vector (column name evaluated within \code{dcce()}).}

\item{k}{Integer lag order. Default 1. Positive values lag, negative lead.}
}
\value{
A numeric vector of the same length as \code{x} with leading \code{NA}s.
}
\description{
Creates a lagged version of a variable for use inside \code{dcce()} formulas.
This function is evaluated during formula processing by \code{dcce()} and should
not be called directly on raw vectors outside of a dcce formula context.
}
\examples{
# Inside a dcce() formula:
# dcce(data, "id", "t", y ~ L(x, 1) + L(x, 2), model = "mg")
}
