% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{dcce_bootstrap}
\alias{dcce_bootstrap}
\title{Bootstrap alias that avoids the broom conflict}
\usage{
dcce_bootstrap(
  object,
  type = c("crosssection", "wild"),
  reps = 500L,
  percentile = TRUE,
  cfresiduals = FALSE,
  seed = NULL
)
}
\arguments{
\item{object}{A \code{dcce_fit} object.}

\item{type}{Character: \code{"crosssection"} (default) or \code{"wild"}.}

\item{reps}{Integer: number of bootstrap repetitions. Default 500.}

\item{percentile}{Logical: compute percentile CIs? Default \code{TRUE}.}

\item{cfresiduals}{Logical: for wild bootstrap, use common-factor
residuals instead of defactored residuals? Default \code{FALSE}.}

\item{seed}{Integer: random seed for reproducibility. Default \code{NULL}.}
}
\value{
See \code{\link{bootstrap}}.
}
\description{
Convenience alias for \code{\link{bootstrap}} with an unambiguous
name. \code{broom::bootstrap} is a data-frame resampling helper that
shares a name with \code{dcce::bootstrap} but has a completely
different signature. If you load \code{broom} after \code{dcce} the
broom function masks ours on the search path and calls to
\code{bootstrap(fit, type = ..., reps = ...)} will fail with an
"unused arguments" error. \code{dcce_bootstrap()} is identical to
\code{dcce::bootstrap()} but cannot be masked by any other package.
}
\examples{
set.seed(42)
df <- data.frame(
  id = rep(1:10, each = 30),
  t  = rep(1:30, 10),
  y  = rnorm(300),
  x  = rnorm(300)
)
fit <- dcce(df, "id", "t", y ~ x, model = "mg", cross_section_vars = NULL)
dcce_bootstrap(fit, reps = 50)
}
\seealso{
\code{\link{bootstrap}}
}
